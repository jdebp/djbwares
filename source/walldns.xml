<?xml version="1.0" encoding="UTF-8"?>
<!-- **************************************************************************
.... For copyright and licensing terms, see the file named COPYING.
.... **************************************************************************
.-->
<?xml-stylesheet href="docbook-xml.css" type="text/css"?>

<refentry id="walldns">

<refmeta xmlns:xi="http://www.w3.org/2001/XInclude">
<refentrytitle>walldns</refentrytitle>
<manvolnum>1</manvolnum>
<refmiscinfo class="manual">user commands</refmiscinfo>
<refmiscinfo class="source">djbwares</refmiscinfo>
<xi:include href="version.xml" />
</refmeta>

<refnamediv>
<refname>walldns</refname>
<refpurpose>an opaque content DNS server using the UDP protocol</refpurpose>
</refnamediv>

<refsynopsisdiv>
<cmdsynopsis>
<command>walldns</command>
</cmdsynopsis>
</refsynopsisdiv>

<refsection>
<title>Description</title>

<para>
<command>walldns</command> is a content DNS server that speaks the DNS/UDP protocol.
It accepts DNS queries from hosts around the Internet, and responds with fixed information.
The queries ask about various IP addresses, taking the form of a reverse lookup, to which it supplies generic responses that avoid revealing local host information.
</para>

<para>
When it starts <command>walldns</command> changes its root to the directory specified by the <envar>ROOT</envar> environment variable, and drops privileges to run as the user ID and group ID specified by the <envar>UID</envar> and <envar>GID</envar> environment variables.
The latter can be set up with <citerefentry><refentrytitle>envuidgid</refentrytitle><manvolnum>1</manvolnum></citerefentry>.
</para>

<para>
Normally <command>walldns</command> is run via a server program such as <command>udp-socket-listen</command> to listen for DNS/UDP queries from hosts around the Internet.
It understands the <envar>LISTEN_PID</envar> and <envar>LISTEN_FDS</envar> environment variable convention for having an already-listening socket passed to it by such a program, and uses the last open file descriptor in the list that refers to a UDP/IPv4 socket.
If no such open file descriptor is provided it falls back to opening its own UDP/IPv4 socket, bound to port 53 of the IP address given by the value of the <envar>IP</envar> environment variable. 
It does not handle DNS/TCP.
</para>

<para>
<command>walldns</command> rejects zone-transfer requests, inverse queries, non-Internet-class queries, truncated packets, packets that contain anything other than a single query, query types other than A, PTR, or ANY, and queries for domain names not beneath <filename>in-addr.arpa</filename> or <filename>ip6.arpa</filename>.
</para>

<para>
<code>ANY</code> queries are answered with a single synthesized <code>HINFO</code> resource record set.
<command>walldns</command> otherwise answers queries in a fixed form.
It does not include any <code>NS</code> or <code>SOA</code> resource records in its responses; and uses TTLs of one week.
</para>

<para>
<command>walldns</command> creates a bidirectional mapping that maps IP addresses to domain names that map back to those same IP addresses.
</para>

<itemizedlist>
<listitem>
<para>
For IP version 4, reverse lookup domain names take the form <filename>d.c.b.a.in-addr.arpa</filename>, where <filename>a.b.c.d</filename> is the IP address being looked up.
<command>walldns</command> publishes <code>PTR</code> responses mapping the domain name back to itself.
It publishes <code>A</code> responses mapping the domain name to the IP address <filename>a.b.c.d</filename>.
</para>
</listitem>
<listitem>
<para>
For IP version 6, reverse lookup domain names take the form <filename>5.4.3.2.1.0.z.y.x.w.v.u.t.s.r.q.p.o.n.m.l.k.j.i.h.g.f.e.d.c.b.a.ip6.arpa</filename>, where <filename>abcd:efgh:ijkl:mnop:qrst:uvwx:yz01:2345</filename> is the IP address being looked up.
<command>walldns</command> publishes <code>PTR</code> responses mapping the domain name back to itself.
It publishes <code>AAAA</code> responses mapping the domain name to the IP address <filename>abcd:efgh:ijkl:mnop:qrst:uvwx:yz01:2345</filename>.
</para>
</listitem>
<listitem>
<para>
It <emphasis>does not</emphasis> publish <code>A</code> responses for any IP version 6 lookups, nor <code>AAAA</code> responses for any IP version 4 addresses, even for IP version 4 "mapped" IP version 6 addresses.
</para>
</listitem>
</itemizedlist>

</refsection>

<refsection>
<title>History</title>
<para>
<command>walldns</command> was originally part of <personname><firstname>Daniel</firstname> <othername>J.</othername> <surname>Bernstein</surname></personname>'s djbdns toolset in 2000.
</para>
<para>
<command>walldns</command> gained IP version 6 support in 2019.
</para>
</refsection>

<refsection>
<title>Author</title>
<para>
Original code and documentation by <personname><firstname>Daniel</firstname> <othername>J.</othername> <surname>Bernstein</surname></personname>.
Documentation modernizations by <personname><firstname>Jonathan</firstname> <surname>de Boyne Pollard</surname></personname>.
</para>
</refsection>

</refentry>
